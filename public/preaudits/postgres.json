{
  "generated_at": "2026-05-20T16:43:52Z",
  "target": "/home/yin/security-research/openclaw-workspace/gemma-agent-surface-map/examples/mcp-catalog/postgres",
  "scanned_files": 2,
  "risk_score": 15,
  "category_counts": {
    "secret_reference": 1
  },
  "rule_counts": {
    "database_credential_surface": 2
  },
  "mcp_servers": [
    {
      "name": "postgres",
      "path": "mcp.json",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres"
      ],
      "env_keys": [
        "DATABASE_URL"
      ],
      "risk_hints": [
        "package runner",
        "credential reference",
        "database access"
      ]
    }
  ],
  "findings": [
    {
      "category": "secret_reference",
      "severity": "medium",
      "path": "mcp.json",
      "line": 7,
      "evidence": "\"DATABASE_URL\": \"postgres://readonly:<redacted>@localhost:5432/app\"",
      "recommendation": "Reference secret names only in reports; never send values to model context."
    }
  ],
  "rules": [
    {
      "id": "database_connection_reference",
      "category": "database_credential_surface",
      "severity": "medium",
      "score": 6,
      "path": "mcp.json",
      "line": 5,
      "evidence": "\"args\": [\"-y\", \"@modelcontextprotocol/server-postgres\"],",
      "recommendation": "Use read-only database users, local replicas, query limits, and no production credentials by default."
    },
    {
      "id": "database_connection_reference",
      "category": "database_credential_surface",
      "severity": "medium",
      "score": 6,
      "path": "mcp.json",
      "line": 7,
      "evidence": "\"DATABASE_URL\": \"postgres://readonly:<redacted>@localhost:5432/app\"",
      "recommendation": "Use read-only database users, local replicas, query limits, and no production credentials by default."
    }
  ],
  "gemma_review": {
    "summary": "This local scan found agent-operating-surface signals that deserve review before broad agent automation.",
    "install_verdict": "add_carefully",
    "confidence": "low",
    "why_gemma_changed_the_call": "Gemma was not used; deterministic fallback kept the static install posture.",
    "agent_constraints": [
      "Do not execute repository code during review.",
      "Keep secret values out of prompts, reports, and logs.",
      "Pass secret names by reference only; never expose values to the model.",
      "Use read-only database users and avoid production credentials by default."
    ],
    "top_risks": [
      "database credential surface appeared 2 time(s).",
      "secret reference appeared 1 time(s)."
    ],
    "quick_wins": [
      "Run read-only before granting write access.",
      "Document which MCP servers and browser profiles are allowed.",
      "Keep secret values out of model context and reports."
    ],
    "hardening_plan": [
      "Inventory agent config and instruction files.",
      "Narrow shell, browser, network, and write permissions.",
      "Re-run the scan and compare the risk score before shipping."
    ]
  },
  "profile": {
    "id": "postgres",
    "name": "Postgres MCP",
    "family": "Database",
    "why_use_it": "Lets an agent query application data or development databases.",
    "pre_audit_note": "Use read-only database users, local replicas, query limits, and no production credentials by default."
  },
  "review_source": "fallback"
}
